ul.tabs.mb-3
  li class=(-if params[:active_tab].eql? "photos" or params[:active_tab].nil? then "tab-link current" else "tab-link" end) data-tab="tab-photo" 
    = link_to t('title.photos'), photos_path, :"data-turbolinks" => false
  li class=(-if params[:active_tab].eql? "albums" then "tab-link current" else "tab-link" end)  data-tab="tab-album" 
    = link_to t('title.albums'), albums_path, :"data-turbolinks" => false